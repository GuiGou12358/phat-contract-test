# Subquery Indexer contracts
Phat Contract that reads the data from the index subquery used by the dApp Lucky

### Build the contract ###
```bash
cargo contract build
```

## Runs the tests
```bash
cargo test -- --nocapture
```